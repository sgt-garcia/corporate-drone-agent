import "./styles.css";

export default function App() {
  return (
    <main className="app-shell">
      <section className="empty-state" aria-labelledby="app-title">
        <h1 id="app-title">Corporate Drone Agent</h1>
        <p>Spring Boot and React are wired together.</p>
      </section>
    </main>
  );
}
